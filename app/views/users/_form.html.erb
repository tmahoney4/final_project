<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :class_number %>
    <%= form.text_field :class_number, id: :user_class_number %>
  </div>

  <div class="field">
    <%= form.label :class_name %>
    <%= form.text_field :class_name, id: :user_class_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
